version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/fyp
hooks:
  BeforeInstall:
    - location: /install_dependencies
      timeout: 30
      runas: root
    - location: /start_server
      timeout: 30
      runas: root
  ApplicationStop:
    - location: /stop_server
      timeout: 30
      runas: root
